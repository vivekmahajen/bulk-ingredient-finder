{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "Single source of truth for cross-language shared constants and enums. TypeScript (zod) lives in src/index.ts; Python (Pydantic) lives in apps/api/app/shared.py. Keep all three in sync. As the domain model lands (PR-2+), entity schemas are added here first.",
  "title": "RasoiRadarShared",
  "constants": {
    "APP_NAME": "Rasoi Radar",
    "API_VERSION": "api/v1"
  },
  "enums": {
    "Locale": ["en", "hi", "es", "zh", "vi", "ko", "pt"],
    "Role": ["owner", "manager", "staff"]
  }
}
